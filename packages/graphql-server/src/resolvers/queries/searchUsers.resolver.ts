import { QueryResolvers } from "../../generated/codegen";

const searchUsersResolver: QueryResolvers.SearchUsersResolver = async (
  parent,
  args,
  ctx
) => {
  throw new Error("Not implemented");
};

export default searchUsersResolver;
